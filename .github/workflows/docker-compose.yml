version: '3'

services:
  bot:
    image: python:3.11-slim
    working_dir: /app
    volumes:
      - .:/app
    command:
      - /bin/sh
      - -c
      - "pip install telethon && python main.py"
    tty: true
    stdin_open: true
    restart: always
